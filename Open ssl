ElasticSearch/8.11.2

public key

openssl pkcs12 -in http.p12 -clcerts -nokeys -out http.crt


private key

openssl pkcs12 -in http.p12 -nocerts -out http.pem

openssl pkcs12 -in transport.p12 -nocerts -out transport.pem
